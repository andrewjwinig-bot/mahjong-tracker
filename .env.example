# Cloud backend (Supabase) — OPTIONAL.
# When BOTH are set, the app can enable accounts + cross-device sync.
# Leave them unset (the default) to keep the app fully on-device.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
